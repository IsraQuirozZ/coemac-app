import { Text } from "react-native";

export default function ManageUsers() {
  return (
    <Text style={{ fontSize: 18, fontWeight: "bold", padding: 100 }}>
      Administrar usuarios (solo para admins)
    </Text>
  );
}
